export FLASK_APP=flaskr
export FLASK_ENV=development

flask run
flask init-db

If you have setup.py and MANIFEST.in:
Install the Project:
Use pip to install your project in the virtual environment.
pip install -e .
